<section class="contact-cta">
  <div class="contact-cta-inner">
    <h2 class="contact-cta-title">Contact</h2>
    <p class="contact-cta-description">お仕事のご依頼・ご相談など、お気軽にどうぞ。</p>
    <a href="/contact" class="contact-cta-button">Contact →</a>
  </div>
</section>

<style lang="sass">
  @use '../styles/spacing' as *
  .contact-cta
    background-color: var(--c-black)
    padding: space(16) space(16)

  .contact-cta-inner
    max-width: 1200px
    margin: space(0) auto
    display: flex
    flex-direction: column
    align-items: center
    text-align: center
    gap: space(6)

  .contact-cta-title
    color: var(--c-white)
    font-size: 40px
    letter-spacing: 0.08em

  .contact-cta-description
    color: var(--c-gray-500)
    font-size: 16px
    line-height: 2
    letter-spacing: 0.04em

  .contact-cta-button
    display: inline-flex
    align-items: center
    gap: space(2)
    margin-top: space(2)
    padding: space(3) space(8)
    border: 1px solid var(--c-accent)
    border-radius: 999px
    color: var(--c-accent)
    font-family: 'Zen Kaku Gothic New', sans-serif
    font-size: 15px
    font-weight: 700
    letter-spacing: 0.08em
    text-decoration: none
    transition: background-color 0.3s ease, color 0.3s ease

    &:hover
      background-color: var(--c-accent)
      color: var(--c-black)

  @media (max-width: 768px)
    .contact-cta
      padding: space(16) space(6)

    .contact-cta-title
      font-size: 32px

    .contact-cta-description
      font-size: 15px
</style>
