export const dg4Color: {
  cyan: string;
  black: string;
  blue: string;
  orange: string;
} = {
  cyan: "#00d4d4",
  black: "#444444",
  blue: "#0044dd",
  orange: "#d44d00",
};

export const cWidth: {
  pc: string;
  tab: string;
  sp: string;
} = {
  pc: "800px",
  tab: "calc(100% - (20px * 2))",
  sp: "calc(100% - (12px * 2))",
};

export const bp: {
  tab: string;
  sp: string;
} = {
  tab: "@media (max-width: 1024px)",
  sp: "@media (max-width: 640px)",
};
