import { css } from "@emotion/react";

const common: {
  flex: any;
  center: any;
} = {
  flex: css`
    display: flex;
  `,
  center: css`
    margin-left: auto;
    margin-right: auto;
  `,
};

export default common;
